{
  "name": "retext-metaphone",
  "version": "0.1.0",
  "description": "Retext implementation of the Metaphone algorithm",
  "license": "MIT",
  "keywords": [
    "metaphone",
    "retext"
  ],
  "dependencies": {
    "metaphone": "^0.1.1"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/wooorm/retext-metaphone.git"
  },
  "author": "Titus Wormer <tituswormer@gmail.com>",
  "devDependencies": {
    "eslint": "^0.8.0",
    "istanbul": "^0.3.0",
    "jscs": "^1.6.0",
    "mocha": "^1.20.0",
    "retext": "^0.2.0-rc.2",
    "retext-content": "^0.2.2",
    "retext-porter-stemmer": "^0.1.1",
    "retext-visit": "^0.1.1"
  },
  "scripts": {
    "test": "node_modules/.bin/_mocha --reporter spec --check-leaks -u exports spec/retext-metaphone.spec.js",
    "test-travis": "node_modules/.bin/istanbul cover node_modules/.bin/_mocha --report lcovonly -- --reporter spec --check-leaks -u exports spec/retext-metaphone.spec.js",
    "lint": "npm run lint-api && npm run lint-test && npm run lint-style",
    "lint-api": "node_modules/.bin/eslint index.js --env node --env browser --rule 'quotes: [2, single]'",
    "lint-test": "node_modules/.bin/eslint spec/retext-metaphone.spec.js --env node --env mocha --rule 'quotes: [2, single]'",
    "lint-style": "node_modules/.bin/jscs index.js spec/retext-metaphone.spec.js --reporter=inline",
    "install-browser-test": "npm install browserify",
    "build-browser-test": "node_modules/.bin/browserify spec/retext-metaphone.spec.js -o spec/browser.spec.js",
    "coverage": "node_modules/.bin/istanbul cover node_modules/.bin/_mocha -- -- spec/retext-metaphone.spec.js",
    "make": "npm run lint && npm run coverage"
  }
}
